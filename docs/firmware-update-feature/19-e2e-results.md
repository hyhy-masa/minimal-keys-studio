# 19. Windows 実機 E2E 結果記録（v0.4.0 修正版）

> 目的: 異常系3問題（A 誤分類 / B 版不明 / C 無確認書込）の修正版を Windows 実機で検証し、publish 可否を判断する。
> 手順の正本は `18-final-fix-plan-fable5.md` §6。本ファイルは実機で ○/× を埋める記録票。

## 実施メタ情報（実施時に記入）

| 項目 | 内容 |
|---|---|
| 対象ビルド | `minimal-keys._0.4.0_x64-setup.exe`（タグ v0.4.0・修正版 `81f1521` 系のCIビルド） |
| Studio 表示バージョン | v0.4.0 |
| 実施日 | 2026-__-__ |
| 実施者 | まさかず |
| Windows 機 | （OS ビルド番号など） |
| 実機キーボード | minimal-keys R + L（各1台） |
| FW / manifest | farmware v1.0.0（公開済・manifest URL 302 応答） |

## 判定ルール
- **E1〜E9 の全てが PASS で publish 可**（E10/E11 は可能なら実施・必須ではない）。
- 1件でも FAIL があれば publish 保留 → 原因記録 → 修正へ差し戻す。

---

## E1 — 正常系フル（R→L→checklist→done）
**操作**: ファーム更新ボタン → 「最新を確認」→ R をブートローダにして書込 → L → チェックリスト → 完了。
**期待**: guide で待機 → **リセット2回** → **確認画面（新規文言）** → 「このまま進める」を押して**初めて書込**開始。done に「バージョン v◯ を書き込みました」が **BLE 切断のまま**表示される。

- [ ] PASS / [ ] FAIL
- ドライブレター（R / L）:
- スクショ:
- メモ:

---

## E2 — 進捗0%帯（preflight窓）で抜線
**操作**: r_flashing の進捗0%帯でケーブルを抜く。
**期待**: 「接続が切れたようです」（**ConnectionLost**。NotUf2Volume ではない）→ 挿し直し → recovery ②R → 完走。

- [ ] PASS / [ ] FAIL
- 表示された文言（ConnectionLost か NotUf2Volume か）:
- スクショ:
- メモ:

---

## E3 — bootloader 残留のまま更新開始（問題C再現・**R残留とL残留の両方**）
**操作**: 事前に片側をブートローダ（USBメモリ化）状態にしたまま更新を開始。**R 残留**と **L 残留**の2パターンで実施。
**期待**: **自動書込されず、確認画面（existing 文言）**が出る。L 残留 × R 工程では文言「反対側なら差し替え」に従い差し替えで正しく継続できる。書込 asset・写真・文言の **side（R/L）一致**を確認。

- R残留: [ ] PASS / [ ] FAIL
- L残留: [ ] PASS / [ ] FAIL
- side（R/L）表示・写真・文言の一致:
- スクショ:
- メモ:

---

## E4 — recovery ② の完走（A症状2再現）
**操作**: 書込中に抜線 → ConnectionLost → ［対処方法を見る］→ ②R → 完走。
**期待**: 完走する。かつ **error 画面と recovery 画面の取り違え**が改名で解消していること（同名だった見出しが区別できる）。

- [ ] PASS / [ ] FAIL
- error/recovery の画面が区別できたか:
- スクショ:
- メモ:

---

## E5 — 書込後半（75%以降）で抜線（偽成功の既知制約）
**操作**: 書込が 75% を超えてからケーブルを抜く。
**期待**: swap_to_l に進み得ることを記録 → **チェックリストで人間が検出** → recovery で復旧できる。

- [ ] PASS / [ ] FAIL
- 挙動（swap_to_l に進んだか／checklist で気づけたか）:
- スクショ:
- メモ:

---

## E6 — 更新前 BLE 接続で版表示（B-1 回帰）
**操作**: 更新前にキーボードを BLE 接続した状態で「最新を確認」。次に未接続でも確認。
**期待**: 接続時は**版が数字表示**（B-1 回帰確認）。未接続では「確認できません（更新には支障ありません）」。

- [ ] PASS / [ ] FAIL
- 接続時の版表示:
- 未接続時の文言:
- スクショ:
- メモ:

---

## E7 — 同一ドライブレター（M-5）
**操作**: guide 進入前に抜き差しして**同じドライブレター**に再マウントさせる。
**期待**: 確認画面が出て進められる（**タイムアウトに詰まらない**）。実レターをログへ記録。

- [ ] PASS / [ ] FAIL
- 実ドライブレター:
- タイムアウト/詰まりの有無:
- スクショ:
- メモ:

---

## E8 — 確認画面表示中に抜線 →「このまま進める」
**操作**: 確認画面が出ている間にケーブルを抜き、「このまま進める」を押す。
**期待**: ConnectionLost → error → recovery（契約表どおりの遷移）。

- [ ] PASS / [ ] FAIL
- 遷移（ConnectionLost→error→recovery か）:
- スクショ:
- メモ:

---

## E9 — 複数台接続
**操作**: (前者) 確認待ち中に2台目を接続して進める。(後者) guide 進入時から2台接続。
**期待**: 前者 = **取得済み側に正常書込**（2台目は無関係）。後者 =「**片方だけ接続**」文言が recovery に表示。

- 前者: [ ] PASS / [ ] FAIL
- 後者: [ ] PASS / [ ] FAIL
- スクショ:
- メモ:

---

## E10 —（任意）foreign UF2 併用
**操作**: RPI-RP2 等の別 UF2 ボリュームを併用（用意できれば）。
**期待**: 無視して待機継続（C-fix-3）。

- [ ] PASS / [ ] FAIL / [ ] SKIP
- メモ:

## E11 —（任意）一過性 INFO 未準備
**操作**: 一過性の INFO 未準備状態（再現困難なら省略可）。
**期待**: 誤 ConnectionLost / NotUf2Volume にならない（unit M-1 で主担保）。

- [ ] PASS / [ ] FAIL / [ ] SKIP
- メモ:

---

## 総合判定

| 判定 | 条件 | 結果 |
|---|---|---|
| **publish 可** | E1〜E9 全て PASS | [ ] |
| **保留** | いずれか FAIL | [ ] |

- publish 判断:
- 次アクション:
